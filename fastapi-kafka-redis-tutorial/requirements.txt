# FastAPI와 웹 서버 관련
fastapi==0.104.1          # 현대적인 Python 웹 프레임워크
uvicorn[standard]==0.24.0 # ASGI 웹 서버 (FastAPI 실행용)

# 데이터 검증 및 설정
pydantic==2.5.0           # 데이터 검증 라이브러리 (FastAPI에서 사용)
pydantic-settings==2.0.3  # 환경변수와 설정 관리
email-validator==2.1.0    # 이메일 검증 (Pydantic EmailStr용)

# 데이터베이스 관련 (PostgreSQL)
sqlalchemy==2.0.23        # Python SQL 툴킷 및 ORM
asyncpg==0.29.0           # 비동기 PostgreSQL 드라이버
psycopg2-binary==2.9.9    # PostgreSQL 어댑터 (동기 작업용)
alembic==1.13.0           # 데이터베이스 마이그레이션 도구

# Redis 관련
redis==5.0.1              # Redis Python 클라이언트
aioredis==2.0.1           # 비동기 Redis 클라이언트 (선택사항)

# Kafka 관련
kafka-python==2.0.2       # Apache Kafka Python 클라이언트

# HTTP 클라이언트 (테스트용)
httpx==0.25.2             # 비동기 HTTP 클라이언트

# 유틸리티
python-dotenv==1.0.0      # .env 파일 로드 (환경변수 관리)
python-multipart==0.0.6   # 파일 업로드 처리 (FastAPI에서 사용)
bcrypt==4.1.2             # 비밀번호 해싱 (보안 강화)
python-jose[cryptography]==3.3.0  # JWT 토큰 처리

# 로깅
loguru==0.7.2             # 더 나은 로깅 라이브러리

# 개발용 도구 (선택사항)
pytest==7.4.3            # 테스트 프레임워크
pytest-asyncio==0.21.1   # 비동기 테스트 지원
pytest-cov==4.1.0        # 테스트 커버리지