# .env 파일 - 환경변수 설정
# 이 파일에서 실제 환경에 맞는 값들을 설정할 수 있습니다.
# 보안상 중요한 정보는 여기에 저장하고 git에 커밋하지 마세요!

# 애플리케이션 설정
APP_NAME="FastAPI Kafka Redis PostgreSQL Tutorial"
APP_VERSION="2.0.0"
DEBUG=true

# PostgreSQL 데이터베이스 설정
DATABASE_URL=postgresql+asyncpg://tutorial_user:tutorial_password@localhost:5432/fastapi_tutorial
DATABASE_SYNC_URL=postgresql://tutorial_user:tutorial_password@localhost:5432/fastapi_tutorial

# 데이터베이스 연결 풀 설정
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=0
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800

# Redis 설정
REDIS_URL=redis://localhost:6379
REDIS_DB=0
REDIS_MAX_CONNECTIONS=10

# Kafka 설정
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_GROUP_ID=fastapi_group
KAFKA_AUTO_OFFSET_RESET=latest

# JWT 및 보안 설정 (실제 운영시에는 안전한 키로 변경하세요!)
SECRET_KEY=your-super-secret-key-change-this-in-production-must-be-at-least-32-characters
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 비밀번호 해싱 설정
PASSWORD_HASH_ROUNDS=12